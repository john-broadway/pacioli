{
  "identifier": "john-broadway-pacioli",
  "name": "Pacioli — the governed front door to your ERPNext books",
  "description": "Governed agent access to your ERPNext books — 51 doctypes across accounts, stock, assets, and manufacturing: every write PLANNED, CONSENTED, PROVEN, and UNDOable. No debit without a credit. MCP + A2A doors, one spine.",
  "version": "0.31.0",
  "tools": [
    {
      "name": "get_sales_invoice",
      "description": "Read one Sales Invoice (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_sales_invoices",
      "description": "List Sales Invoices (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_sales_invoice",
      "description": "Submit a Sales Invoice under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_sales_invoice",
      "description": "Cancel a submitted Sales Invoice under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_sales_invoice",
      "description": "Create the corrected re-draft of a CANCELLED Sales Invoice (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_purchase_invoice",
      "description": "Read one Purchase Invoice (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_purchase_invoices",
      "description": "List Purchase Invoices (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_purchase_invoice",
      "description": "Submit a Purchase Invoice under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_purchase_invoice",
      "description": "Cancel a submitted Purchase Invoice under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_purchase_invoice",
      "description": "Create the corrected re-draft of a CANCELLED Purchase Invoice (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_payment_entry",
      "description": "Read one Payment Entry (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_payment_entries",
      "description": "List Payment Entries (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_payment_entry",
      "description": "Submit a Payment Entry under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_payment_entry",
      "description": "Cancel a submitted Payment Entry under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_payment_entry",
      "description": "Create the corrected re-draft of a CANCELLED Payment Entry (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_journal_entry",
      "description": "Read one Journal Entry (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_journal_entries",
      "description": "List Journal Entries (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_journal_entry",
      "description": "Submit a Journal Entry under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_journal_entry",
      "description": "Cancel a submitted Journal Entry under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_journal_entry",
      "description": "Create the corrected re-draft of a CANCELLED Journal Entry (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_sales_order",
      "description": "Read one Sales Order (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_sales_orders",
      "description": "List Sales Orders (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_sales_order",
      "description": "Submit a Sales Order under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_sales_order",
      "description": "Cancel a submitted Sales Order under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_sales_order",
      "description": "Create the corrected re-draft of a CANCELLED Sales Order (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_purchase_order",
      "description": "Read one Purchase Order (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_purchase_orders",
      "description": "List Purchase Orders (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_purchase_order",
      "description": "Submit a Purchase Order under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_purchase_order",
      "description": "Cancel a submitted Purchase Order under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_purchase_order",
      "description": "Create the corrected re-draft of a CANCELLED Purchase Order (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_material_request",
      "description": "Read one Material Request (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_material_requests",
      "description": "List Material Requests (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_material_request",
      "description": "Submit a Material Request under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_material_request",
      "description": "Cancel a submitted Material Request under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_material_request",
      "description": "Create the corrected re-draft of a CANCELLED Material Request (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_delivery_note",
      "description": "Read one Delivery Note (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_delivery_notes",
      "description": "List Delivery Notes (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_delivery_note",
      "description": "Submit a Delivery Note under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_delivery_note",
      "description": "Cancel a submitted Delivery Note under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_delivery_note",
      "description": "Create the corrected re-draft of a CANCELLED Delivery Note (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_purchase_receipt",
      "description": "Read one Purchase Receipt (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_purchase_receipts",
      "description": "List Purchase Receipts (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_purchase_receipt",
      "description": "Submit a Purchase Receipt under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_purchase_receipt",
      "description": "Cancel a submitted Purchase Receipt under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_purchase_receipt",
      "description": "Create the corrected re-draft of a CANCELLED Purchase Receipt (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_stock_entry",
      "description": "Read one Stock Entry (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_stock_entries",
      "description": "List Stock Entries (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_stock_entry",
      "description": "Submit a Stock Entry under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_stock_entry",
      "description": "Cancel a submitted Stock Entry under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_stock_entry",
      "description": "Create the corrected re-draft of a CANCELLED Stock Entry (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_supplier_quotation",
      "description": "Read one Supplier Quotation (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_supplier_quotations",
      "description": "List Supplier Quotations (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_supplier_quotation",
      "description": "Submit a Supplier Quotation under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_supplier_quotation",
      "description": "Cancel a submitted Supplier Quotation under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_supplier_quotation",
      "description": "Create the corrected re-draft of a CANCELLED Supplier Quotation (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_quotation",
      "description": "Read one Quotation (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_quotations",
      "description": "List Quotations (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_quotation",
      "description": "Submit a Quotation under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_quotation",
      "description": "Cancel a submitted Quotation under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_quotation",
      "description": "Create the corrected re-draft of a CANCELLED Quotation (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_pos_invoice",
      "description": "Read one POS Invoice (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_pos_invoices",
      "description": "List POS Invoices (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_pos_invoice",
      "description": "Submit a POS Invoice under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_pos_invoice",
      "description": "Cancel a submitted POS Invoice under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_pos_invoice",
      "description": "Create the corrected re-draft of a CANCELLED POS Invoice (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_dunning",
      "description": "Read one Dunning (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_dunnings",
      "description": "List Dunnings (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_dunning",
      "description": "Submit a Dunning under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_dunning",
      "description": "Cancel a submitted Dunning under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_dunning",
      "description": "Create the corrected re-draft of a CANCELLED Dunning (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_stock_reconciliation",
      "description": "Read one Stock Reconciliation (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_stock_reconciliations",
      "description": "List Stock Reconciliations (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_stock_reconciliation",
      "description": "Submit a Stock Reconciliation under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_stock_reconciliation",
      "description": "Cancel a submitted Stock Reconciliation under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_stock_reconciliation",
      "description": "Create the corrected re-draft of a CANCELLED Stock Reconciliation (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_landed_cost_voucher",
      "description": "Read one Landed Cost Voucher (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_landed_cost_vouchers",
      "description": "List Landed Cost Vouchers (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_landed_cost_voucher",
      "description": "Submit a Landed Cost Voucher under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_landed_cost_voucher",
      "description": "Cancel a submitted Landed Cost Voucher under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_landed_cost_voucher",
      "description": "Create the corrected re-draft of a CANCELLED Landed Cost Voucher (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_request_for_quotation",
      "description": "Read one Request for Quotation (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_request_for_quotations",
      "description": "List Request for Quotations (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_request_for_quotation",
      "description": "Submit a Request for Quotation under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_request_for_quotation",
      "description": "Cancel a submitted Request for Quotation under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_request_for_quotation",
      "description": "Create the corrected re-draft of a CANCELLED Request for Quotation (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_blanket_order",
      "description": "Read one Blanket Order (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_blanket_orders",
      "description": "List Blanket Orders (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_blanket_order",
      "description": "Submit a Blanket Order under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_blanket_order",
      "description": "Cancel a submitted Blanket Order under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_blanket_order",
      "description": "Create the corrected re-draft of a CANCELLED Blanket Order (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_job_card",
      "description": "Read one Job Card (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_job_cards",
      "description": "List Job Cards (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_job_card",
      "description": "Submit a Job Card under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_job_card",
      "description": "Cancel a submitted Job Card under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_job_card",
      "description": "Create the corrected re-draft of a CANCELLED Job Card (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_bom",
      "description": "Read one BOM (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_boms",
      "description": "List BOMs (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_bom",
      "description": "Submit a BOM under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_bom",
      "description": "Cancel a submitted BOM under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_bom",
      "description": "Create the corrected re-draft of a CANCELLED BOM (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_work_order",
      "description": "Read one Work Order (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_work_orders",
      "description": "List Work Orders (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_work_order",
      "description": "Submit a Work Order under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_work_order",
      "description": "Cancel a submitted Work Order under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_work_order",
      "description": "Create the corrected re-draft of a CANCELLED Work Order (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_asset",
      "description": "Read one Asset (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_assets",
      "description": "List Assets (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_asset",
      "description": "Submit a Asset under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_asset",
      "description": "Cancel a submitted Asset under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_asset",
      "description": "Create the corrected re-draft of a CANCELLED Asset (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_packing_slip",
      "description": "Read one Packing Slip (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_packing_slips",
      "description": "List Packing Slips (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_packing_slip",
      "description": "Submit a Packing Slip under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_packing_slip",
      "description": "Cancel a submitted Packing Slip under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_packing_slip",
      "description": "Create the corrected re-draft of a CANCELLED Packing Slip (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_cost_center_allocation",
      "description": "Read one Cost Center Allocation (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_cost_center_allocations",
      "description": "List Cost Center Allocations (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_cost_center_allocation",
      "description": "Submit a Cost Center Allocation under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_cost_center_allocation",
      "description": "Cancel a submitted Cost Center Allocation under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_cost_center_allocation",
      "description": "Create the corrected re-draft of a CANCELLED Cost Center Allocation (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_supplier_scorecard_period",
      "description": "Read one Supplier Scorecard Period (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_supplier_scorecard_periods",
      "description": "List Supplier Scorecard Periods (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_supplier_scorecard_period",
      "description": "Submit a Supplier Scorecard Period under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_supplier_scorecard_period",
      "description": "Cancel a submitted Supplier Scorecard Period under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_supplier_scorecard_period",
      "description": "Create the corrected re-draft of a CANCELLED Supplier Scorecard Period (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_quality_inspection",
      "description": "Read one Quality Inspection (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_quality_inspections",
      "description": "List Quality Inspections (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_quality_inspection",
      "description": "Submit a Quality Inspection under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_quality_inspection",
      "description": "Cancel a submitted Quality Inspection under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_quality_inspection",
      "description": "Create the corrected re-draft of a CANCELLED Quality Inspection (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_installation_note",
      "description": "Read one Installation Note (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_installation_notes",
      "description": "List Installation Notes (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_installation_note",
      "description": "Submit a Installation Note under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_installation_note",
      "description": "Cancel a submitted Installation Note under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_installation_note",
      "description": "Create the corrected re-draft of a CANCELLED Installation Note (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_shipment",
      "description": "Read one Shipment (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_shipments",
      "description": "List Shipments (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_shipment",
      "description": "Submit a Shipment under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_shipment",
      "description": "Cancel a submitted Shipment under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_shipment",
      "description": "Create the corrected re-draft of a CANCELLED Shipment (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_sales_forecast",
      "description": "Read one Sales Forecast (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_sales_forecasts",
      "description": "List Sales Forecasts (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_sales_forecast",
      "description": "Submit a Sales Forecast under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_sales_forecast",
      "description": "Cancel a submitted Sales Forecast under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_sales_forecast",
      "description": "Create the corrected re-draft of a CANCELLED Sales Forecast (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_project_update",
      "description": "Read one Project Update (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_project_updates",
      "description": "List Project Updates (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_project_update",
      "description": "Submit a Project Update under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_project_update",
      "description": "Cancel a submitted Project Update under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_project_update",
      "description": "Create the corrected re-draft of a CANCELLED Project Update (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_maintenance_visit",
      "description": "Read one Maintenance Visit (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_maintenance_visits",
      "description": "List Maintenance Visits (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_maintenance_visit",
      "description": "Submit a Maintenance Visit under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_maintenance_visit",
      "description": "Cancel a submitted Maintenance Visit under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_maintenance_visit",
      "description": "Create the corrected re-draft of a CANCELLED Maintenance Visit (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_maintenance_schedule",
      "description": "Read one Maintenance Schedule (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_maintenance_schedules",
      "description": "List Maintenance Schedules (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_maintenance_schedule",
      "description": "Submit a Maintenance Schedule under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_maintenance_schedule",
      "description": "Cancel a submitted Maintenance Schedule under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_maintenance_schedule",
      "description": "Create the corrected re-draft of a CANCELLED Maintenance Schedule (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_asset_maintenance_log",
      "description": "Read one Asset Maintenance Log (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_asset_maintenance_logs",
      "description": "List Asset Maintenance Logs (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_asset_maintenance_log",
      "description": "Submit a Asset Maintenance Log under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_asset_maintenance_log",
      "description": "Cancel a submitted Asset Maintenance Log under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_asset_maintenance_log",
      "description": "Create the corrected re-draft of a CANCELLED Asset Maintenance Log (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_bank_guarantee",
      "description": "Read one Bank Guarantee (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_bank_guarantees",
      "description": "List Bank Guarantees (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_bank_guarantee",
      "description": "Submit a Bank Guarantee under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_bank_guarantee",
      "description": "Cancel a submitted Bank Guarantee under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_bank_guarantee",
      "description": "Create the corrected re-draft of a CANCELLED Bank Guarantee (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_asset_movement",
      "description": "Read one Asset Movement (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_asset_movements",
      "description": "List Asset Movements (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_asset_movement",
      "description": "Submit a Asset Movement under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_asset_movement",
      "description": "Cancel a submitted Asset Movement under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_asset_movement",
      "description": "Create the corrected re-draft of a CANCELLED Asset Movement (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_delivery_trip",
      "description": "Read one Delivery Trip (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_delivery_trips",
      "description": "List Delivery Trips (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_delivery_trip",
      "description": "Submit a Delivery Trip under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_delivery_trip",
      "description": "Cancel a submitted Delivery Trip under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_delivery_trip",
      "description": "Create the corrected re-draft of a CANCELLED Delivery Trip (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_asset_value_adjustment",
      "description": "Read one Asset Value Adjustment (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_asset_value_adjustments",
      "description": "List Asset Value Adjustments (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_asset_value_adjustment",
      "description": "Submit a Asset Value Adjustment under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_asset_value_adjustment",
      "description": "Cancel a submitted Asset Value Adjustment under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_asset_value_adjustment",
      "description": "Create the corrected re-draft of a CANCELLED Asset Value Adjustment (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_payment_order",
      "description": "Read one Payment Order (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_payment_orders",
      "description": "List Payment Orders (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_payment_order",
      "description": "Submit a Payment Order under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_payment_order",
      "description": "Cancel a submitted Payment Order under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_payment_order",
      "description": "Create the corrected re-draft of a CANCELLED Payment Order (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_share_transfer",
      "description": "Read one Share Transfer (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_share_transfers",
      "description": "List Share Transfers (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_share_transfer",
      "description": "Submit a Share Transfer under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_share_transfer",
      "description": "Cancel a submitted Share Transfer under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_share_transfer",
      "description": "Create the corrected re-draft of a CANCELLED Share Transfer (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_bom_creator",
      "description": "Read one BOM Creator (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_bom_creators",
      "description": "List BOM Creators (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_bom_creator",
      "description": "Submit a BOM Creator under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_bom_creator",
      "description": "Cancel a submitted BOM Creator under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_bom_creator",
      "description": "Create the corrected re-draft of a CANCELLED BOM Creator (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_budget",
      "description": "Read one Budget (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_budgets",
      "description": "List Budgets (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_budget",
      "description": "Submit a Budget under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_budget",
      "description": "Cancel a submitted Budget under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_budget",
      "description": "Create the corrected re-draft of a CANCELLED Budget (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_timesheet",
      "description": "Read one Timesheet (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_timesheets",
      "description": "List Timesheets (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_timesheet",
      "description": "Submit a Timesheet under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_timesheet",
      "description": "Cancel a submitted Timesheet under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_timesheet",
      "description": "Create the corrected re-draft of a CANCELLED Timesheet (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_contract",
      "description": "Read one Contract (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_contracts",
      "description": "List Contracts (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_contract",
      "description": "Submit a Contract under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_contract",
      "description": "Cancel a submitted Contract under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_contract",
      "description": "Create the corrected re-draft of a CANCELLED Contract (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_pick_list",
      "description": "Read one Pick List (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_pick_lists",
      "description": "List Pick Lists (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_pick_list",
      "description": "Submit a Pick List under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_pick_list",
      "description": "Cancel a submitted Pick List under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_pick_list",
      "description": "Create the corrected re-draft of a CANCELLED Pick List (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_asset_repair",
      "description": "Read one Asset Repair (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_asset_repairs",
      "description": "List Asset Repairs (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_asset_repair",
      "description": "Submit a Asset Repair under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_asset_repair",
      "description": "Cancel a submitted Asset Repair under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_asset_repair",
      "description": "Create the corrected re-draft of a CANCELLED Asset Repair (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_invoice_discounting",
      "description": "Read one Invoice Discounting (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_invoice_discountings",
      "description": "List Invoice Discountings (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_invoice_discounting",
      "description": "Submit a Invoice Discounting under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_invoice_discounting",
      "description": "Cancel a submitted Invoice Discounting under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_invoice_discounting",
      "description": "Create the corrected re-draft of a CANCELLED Invoice Discounting (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_asset_capitalization",
      "description": "Read one Asset Capitalization (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_asset_capitalizations",
      "description": "List Asset Capitalizations (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_asset_capitalization",
      "description": "Submit a Asset Capitalization under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_asset_capitalization",
      "description": "Cancel a submitted Asset Capitalization under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_asset_capitalization",
      "description": "Create the corrected re-draft of a CANCELLED Asset Capitalization (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_production_plan",
      "description": "Read one Production Plan (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_production_plans",
      "description": "List Production Plans (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_production_plan",
      "description": "Submit a Production Plan under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_production_plan",
      "description": "Cancel a submitted Production Plan under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_production_plan",
      "description": "Create the corrected re-draft of a CANCELLED Production Plan (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_subcontracting_order",
      "description": "Read one Subcontracting Order (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_subcontracting_orders",
      "description": "List Subcontracting Orders (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_subcontracting_order",
      "description": "Submit a Subcontracting Order under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_subcontracting_order",
      "description": "Cancel a submitted Subcontracting Order under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_subcontracting_order",
      "description": "Create the corrected re-draft of a CANCELLED Subcontracting Order (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_subcontracting_inward_order",
      "description": "Read one Subcontracting Inward Order (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_subcontracting_inward_orders",
      "description": "List Subcontracting Inward Orders (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_subcontracting_inward_order",
      "description": "Submit a Subcontracting Inward Order under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_subcontracting_inward_order",
      "description": "Cancel a submitted Subcontracting Inward Order under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_subcontracting_inward_order",
      "description": "Create the corrected re-draft of a CANCELLED Subcontracting Inward Order (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "get_subcontracting_receipt",
      "description": "Read one Subcontracting Receipt (permission-scoped, read-only).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "list_subcontracting_receipts",
      "description": "List Subcontracting Receipts (read-only). Optional Frappe filters, e.g. [[\"status\",\"=\",\"Draft\"]].",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filters": {
            "type": "array",
            "items": {
              "type": "array"
            },
            "description": "Frappe filter triples."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 20
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "submit_subcontracting_receipt",
      "description": "Submit a Subcontracting Receipt under a recorded plan and a live human-minted consent marker. Refuses a stale plan, a period-lock violation, a plan bound to a different document or document type, or a missing/spent/mismatched marker. This is one of the two state-changing tools.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_submit."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "cancel_subcontracting_receipt",
      "description": "Cancel a submitted Subcontracting Receipt under a recorded cancel-plan and a live human-minted consent marker (UNDO — docstatus 1 → 2). Refuses a stale plan, a period-lock violation, a missing/spent/mismatched marker, a plan bound to a different document or document type, or a marker minted for any other operation. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "amend_subcontracting_receipt",
      "description": "Create the corrected re-draft of a CANCELLED Subcontracting Receipt (UNDO's second half): a new DRAFT copied from it with amended_from set. Reversible — nothing posts, deleting the draft undoes it — so no consent marker is required; submitting the corrected draft is the irreversible step and demands its own plan_submit + human-minted marker. Under an active ERPNext Workflow the draft is seated at the workflow's initial state (disclosed as workflow_seat), so it is born with legal transitions. Refuses an uncancelled source, a source that already has an amendment (any docstatus), a wrong-books company, and ambiguous/malformed/unseatable workflow configuration. Writes the intent+outcome receipt pair.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Cancelled document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "workflow_status",
      "description": "Read the ERPNext Workflow (if any) governing this document: whether one is active, the document's current workflow state, the legal transitions from here (each flagged approving vs non-approving, with its role and self-approval setting), and an honest separation-of-duties risk note. Read-only; unrelated to the plan/marker flow. No active workflow returns workflow_active=false, not an error. pacioli_doctype selects the document's DocType (default Sales Invoice).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          },
          "pacioli_doctype": {
            "type": "string",
            "description": "ERPNext DocType to operate on: 'Sales Invoice', 'Purchase Invoice', 'Payment Entry', or 'Journal Entry' (omit for 'Sales Invoice', today's default). A doctype this broker is not built and tested for is refused."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "plan_submit",
      "description": "PLAN a document submit: dry-run the draft via ERPNext's native ledger preview and record the plan. Returns plan_id, the projected GL impact, and risk flags. Nothing is posted. A human then mints a consent marker for the plan_id, out of band. pacioli_doctype selects the document's DocType (default Sales Invoice) — the plan is BOUND to it, and the matching submit_<doctype> tool must be used to consume it.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Draft document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          },
          "pacioli_doctype": {
            "type": "string",
            "description": "ERPNext DocType to operate on: 'Sales Invoice', 'Purchase Invoice', 'Payment Entry', or 'Journal Entry' (omit for 'Sales Invoice', today's default). A doctype this broker is not built and tested for is refused."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "plan_cancel",
      "description": "PLAN a document cancel (UNDO): read the posting's live GL rows (what the cancel unwinds), check the linked-submitted-documents blast radius, and record the plan. Refuses when other submitted documents link to this one (use plan_cascade_cancel to govern the whole graph in one consent). Nothing is cancelled. A human then mints a consent marker for the plan_id, out of band — a cancel marker never authorizes a submit, or vice versa. pacioli_doctype selects the document's DocType (default Sales Invoice) — the plan is BOUND to it.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          },
          "pacioli_doctype": {
            "type": "string",
            "description": "ERPNext DocType to operate on: 'Sales Invoice', 'Purchase Invoice', 'Payment Entry', or 'Journal Entry' (omit for 'Sales Invoice', today's default). A doctype this broker is not built and tested for is refused."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "plan_cascade_cancel",
      "description": "PLAN a CASCADE cancel: discover the full submitted-dependent graph of a document, order it (dependents first, the target last), and record the plan for the WHOLE ordered graph. Refuses a cycle or a graph over the cap. Nothing is cancelled. A human then mints ONE consent marker for the plan_id that authorizes exactly this enumerated graph. pacioli_doctype selects the target's DocType (default Sales Invoice). Any doctype may appear in the graph; each node is labeled 'modeled' (Sales/Purchase Invoice) or 'generic'.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted target document name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          },
          "pacioli_doctype": {
            "type": "string",
            "description": "ERPNext DocType to operate on: 'Sales Invoice', 'Purchase Invoice', 'Payment Entry', or 'Journal Entry' (omit for 'Sales Invoice', today's default). A doctype this broker is not built and tested for is refused."
          }
        },
        "required": [
          "name"
        ]
      }
    },
    {
      "name": "cascade_cancel",
      "description": "Execute a recorded cascade-cancel plan under a live human-minted marker: cancel every document in the frozen graph in order (dependents first, target last, docstatus 1 -> 2). Re-checks the graph is unchanged since planning and each node's freshness + period-lock before its cancel. Fail-stop: on the first failure it STOPS and returns exactly what was cancelled and where it stopped (ok is true only if ALL cancelled). The marker is spent if any document was cancelled. ERPNext's own cancel-blocks are honored, never bypassed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Submitted target document name."
          },
          "plan_id": {
            "type": "string",
            "description": "From plan_cascade_cancel."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "name",
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "plan_reconcile",
      "description": "PLAN a governed reconciliation (settling payments against invoices): the agent proposes specific (payment, invoice, amount) tuples; the broker reads each named invoice and payment FRESH, discloses the settlement (per row: which payment settles which invoice, the amount, live pre-outstanding -> projected post-outstanding), a standing note that reconciliation bypasses ERPNext's own freeze belts (the broker enforces the closed-books refusal itself, ERPNext will not), and a note naming the possible system Journal Entry side-effects (exchange gain/loss; credit/debit note). Nothing is posted. A human then mints a consent marker for the plan_id, out of band — it binds to exactly this pinned allocation set; reconcile never re-reads the allocation from the caller.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "party_type": {
            "type": "string",
            "description": "ERPNext party type, e.g. 'Customer' or 'Supplier'."
          },
          "party": {
            "type": "string",
            "description": "The party name."
          },
          "company": {
            "type": "string",
            "description": "The ERPNext company every allocation must belong to."
          },
          "receivable_payable_account": {
            "type": "string",
            "description": "The GL account (e.g. Debtors/Creditors) reconciliation is scoped to."
          },
          "allocations": {
            "type": "array",
            "description": "The specific settlements proposed: each row names one payment and one invoice to settle it against, and the amount. The broker validates and owns the final allocation graph — it does not auto-match.",
            "items": {
              "type": "object",
              "properties": {
                "payment_type": {
                  "type": "string",
                  "description": "'Payment Entry'. Journal-Entry payments are a deferred extension, refused at plan and execute."
                },
                "payment_no": {
                  "type": "string",
                  "description": "The Payment Entry name."
                },
                "invoice_type": {
                  "type": "string",
                  "description": "'Sales Invoice' or 'Purchase Invoice'."
                },
                "invoice_no": {
                  "type": "string",
                  "description": "The invoice name."
                },
                "allocated_amount": {
                  "type": "number",
                  "description": "The amount to settle."
                }
              },
              "required": [
                "payment_type",
                "payment_no",
                "invoice_type",
                "invoice_no",
                "allocated_amount"
              ]
            }
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "party_type",
          "party",
          "company",
          "receivable_payable_account",
          "allocations"
        ]
      }
    },
    {
      "name": "reconcile",
      "description": "Execute a recorded reconcile plan under a live human-minted marker: settle exactly the pinned allocation set from plan_reconcile. The allocation is NEVER re-supplied here — only plan_id and marker travel — it comes only from the pinned plan graph (the broker's safety control: the agent proposes at plan time, the broker owns and constructs the write). Re-checks per-row freshness, closed-books, and the allocation ceiling against a FRESH read (never the plan's disclosed numbers) before any write; one reconcile call across the whole set; confirms via a readback of each invoice's real post-write outstanding, never the call's own result body.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "plan_id": {
            "type": "string",
            "description": "From plan_reconcile."
          },
          "marker": {
            "type": "string",
            "description": "The single-use consent token a human handed you."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        },
        "required": [
          "plan_id",
          "marker"
        ]
      }
    },
    {
      "name": "request_workflow_transition",
      "description": "Perform a NON-APPROVING ERPNext Workflow transition (e.g. Draft -> Pending Approval) on a document governed by an active Workflow. Refuses any transition whose next state carries docstatus 1 or 2 — that approving transition belongs to a human with the named role, never the broker — and refuses an action that isn't legal from the document's current state, naming the legal ones. Reversible (a workflow-state move, never a docstatus change), so it takes no consent marker, but writes the intent+outcome receipt pair like every other mutation. pacioli_doctype selects the document's DocType (default Sales Invoice).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Document name."
          },
          "action": {
            "type": "string",
            "description": "The workflow transition's action name."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          },
          "pacioli_doctype": {
            "type": "string",
            "description": "ERPNext DocType to operate on: 'Sales Invoice', 'Purchase Invoice', 'Payment Entry', or 'Journal Entry' (omit for 'Sales Invoice', today's default). A doctype this broker is not built and tested for is refused."
          }
        },
        "required": [
          "name",
          "action"
        ]
      }
    },
    {
      "name": "prove_verify",
      "description": "Verify the receipt chain (PROVE). Optionally check the head against an off-box-recorded anchor. Also sweeps any queued-consequence submit (e.g. BOM Creator, whose BOM tree builds on a background queue after submit answers) still marked committed_pending_async, attesting the real Completed/Failed result when the worker has landed it, or reporting it still pending.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "expected_head": {
            "type": "string",
            "description": "Off-box-recorded head hmac."
          },
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    },
    {
      "name": "prove_orphans",
      "description": "Intent receipts with no committed outcome — mutations whose success was never proven; reconcile each against the document's real docstatus.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "pacioli_target": {
            "type": "string",
            "description": "Registry target to route to (omit to use the default target)."
          }
        }
      }
    }
  ]
}
